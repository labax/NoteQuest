{
  "name": "notequest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "24.15.0",
    "npm": "11.4.2"
  },
  "scripts": {
    "dev": "npm run dev -w @notequest/web",
    "build": "npm run typecheck && npm run build -w @notequest/web",
    "preview": "npm run preview -w @notequest/web",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "eslint . --max-warnings=0",
    "static-check": "npm run lint",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm test && npm run build",
    "test:ci": "vitest run",
    "simulation:cli": "node --experimental-strip-types packages/simulation/src/cli.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/jest-dom": "^6.6.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.6.0",
    "eslint": "^10.7.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.7.0",
    "jsdom": "^26.1.0",
    "prettier": "^3.9.5",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.64.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
